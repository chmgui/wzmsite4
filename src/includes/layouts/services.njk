{% extends "layouts/simple.njk" %}

{% block postContent %}


<div class="service-area sp">
    <div class="container">
        <div class="row">
        {% for service in collections.services %}
         <div class="col-lg-4 col-md-6 single-service-2">
                <div class="inner">
                    <div class="title">
                        <div class="icon">
                            <i class="fa fa-{{ service.data.faIcon }}"></i>
                        </div>
                        <h4>{{ service.data.title }}</h4>
                    </div>
                    <div class="content">
                        <p>{{ service.data.description | striptags(true) | escape | nl2br }}  </p>
                       
                       <!--
                        <a href="{{ '{{ service.url }}' | url }}">Read more</a>
                        -->
                               <a href="{{ service.url | url }}">Read more1   </a>
                         <a href="{{ service.url }}">Read more</a>
                    </div>
                </div>
            </div>
        {% endfor %}
         </div>
    </div>
</div>  


          
{% endblock postContent %}